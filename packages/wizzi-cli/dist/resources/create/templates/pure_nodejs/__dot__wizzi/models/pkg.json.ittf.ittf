{
    name "${ctx.name}"
    version "${ctx.version}"
	description "${ctx.description}"
    { author
        name "${ctx.author.name}"
        email "${ctx.author.email}"
	{ license
        type "${ctx.licence}"
        url "${ctx.licence_url}"