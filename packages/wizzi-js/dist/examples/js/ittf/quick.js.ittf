module
    kind es6
    const path = require('path')
    set module.exports =
        =>
            param api
            const isWebpack
                _ api.caller
                    =>
                        param caller
                        _ Boolean(caller && caller.name === 'babel-loader')
            return
                {
