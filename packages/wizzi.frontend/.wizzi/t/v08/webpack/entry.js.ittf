$group
    
    $if wzCtx.Build.useTypescript
        @ './src/index.tsx'
    $else
        @ './src/index.js'
    $if wzCtx.Build.useServiceWorker
        $if wzCtx.Build.useTypescript
            @ sw './src/client/sw.tsx'
                # Service worker
        $else
            @ sw './src/client/sw.tsx'
                # Service worker
