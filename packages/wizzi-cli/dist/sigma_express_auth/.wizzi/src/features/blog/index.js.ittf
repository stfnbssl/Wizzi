module
    kind es6
    $
        var md = {};
        if (wzCtx.Build.useBlog) {
            md.tobeIncluded = true;
            md.feature = wzCtx.Build.blog;
        }
    $if md.tobeIncluded
        v08/express/features/dataModels/index(&md)
