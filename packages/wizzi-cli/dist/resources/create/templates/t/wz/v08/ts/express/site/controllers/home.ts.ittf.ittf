$group

    $
        var lparen = '(';
    
    ${'$'}
        var ctrl = {
            name: 'Home',
            path: ''
        }

    controller${lparen} &ctrl )
        
        _ this.router.get
            `lit 
                + /
            @expr this.home

        ${'$'}append handlers

            handler${lparen} home )
                _ sendHtml
                    @ response
                    @ '<h1>Hello world</h1>'