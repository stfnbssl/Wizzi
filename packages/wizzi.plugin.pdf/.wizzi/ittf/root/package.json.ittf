{
    $
        var md = {
        }
    v08/root/package(&md)
        $append scripts
            examples "node examples"
            first "node examples/dist/first.pdf"
            pre "npm run examples"
            docs "npm run first"
            all "npm run pre && npm run docs"
        $append dependencies
            pdfmake "^0.1.70"
        $append devDependencies

