module
    kind es6
    $
        var call = '(&md)'
    
    ${'$'}
        var md = {
        }

    ${'$'}if wzCtx.Build.useSite
        ${'$'}if wzCtx.Build.site.useViews
            v08/express/site/controllers/home${call}
