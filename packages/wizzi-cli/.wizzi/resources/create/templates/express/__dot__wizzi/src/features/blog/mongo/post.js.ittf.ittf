module
    kind es6
    $
        var call = '(&md)';
    
    ${'$'}
        var md = {
        }

    ${'$'}if wzCtx.Build.useBlog
        ${'$'}if wzCtx.Build.useMongoDb
            v08/express/features/blog/mongo/post${call}
